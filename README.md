# MarkdownPro - 고급 마크다운 에디터

Python과 PyQt6로 만든 기능이 풍부한 마크다운 에디터입니다.

![MarkdownPro Screenshot](screenshot.png)

## ✨ 주요 기능

### 📝 편집 기능
- **실시간 미리보기**: 작성하면서 바로 결과 확인
- **구문 강조**: 마크다운 문법에 따른 색상 표시
- **자동 완성**: 마크다운 문법 자동 완성 지원
- **찾기/바꾸기**: 텍스트 검색 및 치환

### 🎨 UI/UX
- **다크 모드**: 눈이 편한 다크 테마 지원
- **분할 뷰**: 에디터와 미리보기 분할 보기
- **사이드바**: 예제와 가이드 패널
- **툴바**: 자주 쓰는 서식 버튼

### 📚 초보자 지원
- **예제 템플릿**: README, 블로그, 회의록 등 다양한 템플릿
- **치트시트**: 마크다운 문법 가이드 내장
- **툴팁**: 버튼에 마우스 올리면 설명 표시

### 🔧 고급 기능
- **테이블 삽입**: 대화상자로 쉽게 테이블 생성
- **링크/이미지 삽입**: 간편한 미디어 삽입
- **이모지 삽입**: 이모지 선택 패널
- **HTML/PDF 내보내기**: 다양한 형식으로 내보내기
- **자동 저장**: 1분마다 자동 저장
- **최근 파일**: 최근 열었던 파일 목록

## 📦 설치

### 요구사항
- Python 3.9 이상
- PyQt6

### 설치 방법

```bash
# 저장소 다운로드 후 폴더로 이동
cd markdown-editor

# 가상환경 생성 (권장)
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 실행
python markdown_editor.py
```

## 🚀 사용법

### 기본 편집

1. 왼쪽 에디터 영역에 마크다운 입력
2. 오른쪽에서 실시간 미리보기 확인
3. 툴바 버튼으로 서식 적용

### 단축키

| 단축키 | 기능 |
|--------|------|
| `Ctrl+N` | 새 문서 |
| `Ctrl+O` | 열기 |
| `Ctrl+S` | 저장 |
| `Ctrl+Shift+S` | 다른 이름으로 저장 |
| `Ctrl+F` | 찾기/바꾸기 |
| `Ctrl+1` / `2` / `3` | 제목 1/2/3 |
| `Ctrl+B` | 굵게 |
| `Ctrl+I` | 기울임 |
| `Ctrl+K` | 링크 삽입 |
| `Ctrl+`` ` `` | 인라인 코드 |
| `Ctrl+Z` | 실행 취소 |
| `Ctrl+Y` | 다시 실행 |

### 예제 사용하기

1. 왼쪽 사이드바에서 "📚 예제" 탭 선택
2. 원하는 템플릿 선택
3. "에디터에 삽입" 버튼 클릭 또는 더블클릭

### 마크다운 배우기

1. 왼쪽 사이드바에서 "📖 가이드" 탭 선택
2. 각 문법의 예시 확인
3. 복사하여 에디터에 붙여넣기

### 내보내기

**HTML로 내보내기:**
파일 > 내보내기 > HTML로 내보내기

**PDF로 내보내기:**
파일 > 내보내기 > PDF로 내보내기

## 📋 지원 형식

### 마크다운 문법
- 제목 (H1~H6)
- 굵게, 기울임, 취소선
- 순서 있는/없는 목록
- 체크리스트
- 코드 블록 (구문 강조)
- 인용문
- 링크, 이미지
- 테이블
- 수평선

### 확장 기능
- 코드 하이라이팅 (`codehilite`)
- 목차 자동 생성 (`toc`)
- 테이블 (`tables`)
- 펜스 코드 블록 (`fenced_code`)

## 🎨 테마

### 라이트 모드
밝은 배경에 어두운 텍스트. 기본 테마입니다.

### 다크 모드
어두운 배경에 밝은 텍스트. 
- 메뉴: 보기 > 다크 모드
- 설정이 자동 저장됩니다

## 📁 프로젝트 구조

```
markdown-editor/
├── markdown_editor.py   # 메인 프로그램
├── requirements.txt     # 의존성
└── README.md           # 이 파일
```

## 🔧 설정

설정은 `~/.markdownpro_config.json`에 자동 저장됩니다:
- 다크 모드 설정
- 최근 파일 목록

## ⚠️ 문제 해결

### PyQt6-WebEngine 설치 오류

```bash
# macOS
brew install qt@6

# Ubuntu/Debian
sudo apt install python3-pyqt6.qtwebengine

# pip 재설치
pip install --upgrade PyQt6-WebEngine
```

### 한글 깨짐

UTF-8로 파일 저장 확인:
```python
with open(file, 'w', encoding='utf-8') as f:
    f.write(content)
```

### 미리보기가 표시되지 않음

PyQt6-WebEngine이 올바르게 설치되었는지 확인:
```bash
python -c "from PyQt6.QtWebEngineWidgets import QWebEngineView; print('OK')"
```

## 📄 라이선스

MIT License

## 🤝 기여

이슈와 PR을 환영합니다!

1. Fork
2. Feature Branch (`git checkout -b feature/amazing`)
3. Commit (`git commit -m 'Add amazing feature'`)
4. Push (`git push origin feature/amazing`)
5. Pull Request
